vector<range> rs;

double range_sum() {
	sort(rs.begin(), rs.end());
	double S = 0, p = -99999999;
	for (int i = 0; i < (int) rs.size(); ++i) {
		S += max(0.0, rs[i].high - max(p, rs[i].low));
		p = max(p, rs[i].high);
	}
	return S;
}