//si lo que tenemos es un segmento delimitado por 2 ptos
//entonces primero tenemos que encontrar A, B, C para cada segmento
A = y2-y1
B = x1-x2
C = A*x1+B*y1

//pto interseccion
double det = A1*B2 - A2*B1
    if(det == 0){
        //Lines are parallel
    }else{
        double x = (B2*C1 - B1*C2)/det
        double y = (A1*C2 - A2*C1)/det
    }