int ant[MAX],act[MAX];
char a[MAX],b[MAX];
int main(){

	scanf("%s",a);
	scanf("%s",b);
	int la = strlen(a);
	int lb = strlen(b);
	for (int i = 1; i <= la; i++) 
	    ant[i] = i;
	for (int i = 1; i <= lb; i++) {
		act[0] = i;
		for (int j = 1; j <= la; j++) {
			int val = (b[i - 1] == a[j - 1]) ? 0 : 1;
			act[j] = min(ant[j - 1] + val, min(act[j - 1] + 1, ant[j] + 1));
		}
		for (int j = 0; j <= la; j++) 
		    ant[j] = act[j];
	}
	printf("%d",act[la]);
	return 0;
}